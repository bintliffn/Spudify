import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  bottom: {
    backgroundColor: "black",
    opacity: "20%",
    position: "absolute",
    left: 0,
    right: 0,
    bottom: 0,
  },
});
